
quarkus:
   datasource:
      db-kind: mysql 
      jdbc:
        driver: 
        url:  
      username: dcsdsd
      password: ddd
# drop and create the database at startup (use `update` to only update the schema)
   hibernate-orm:
      database:
         generation: update
